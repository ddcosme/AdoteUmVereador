@model IEnumerable<TCC.Models.Encaminhamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dataEnc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.anexo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.rg)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoEncaminhamento.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vereador.partidoPulitico)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dataEnc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.anexo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.rg)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoEncaminhamento.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vereador.partidoPulitico)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
